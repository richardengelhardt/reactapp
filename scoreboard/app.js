const desc = 'Just want to know this pa kaba'
const name = 'Riec'

//create header into the DOM!
const header =(
<header>
    <h1>{ name }</h1>
    <p>{ desc }</p>
</header>

);

ReactDOM.render(
 header,
 document.getElementById('root')
);


//console.log(title);